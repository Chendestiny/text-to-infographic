# Security Policy

## 支持范围

本项目是一个 **Agent 技能 + 本地脚本集合**：读取本地文章与规格文件，
用本机浏览器渲染出图片。它不是网络服务，没有线上部署面。

我们只为**最新提交**处理问题，不为历史版本发补丁。

## 报告问题

请通过 GitHub 的 [Security advisories](../../security/advisories) 私下报告，
**不要**开公开 issue。请附上：

- 能复现的最小规格文件（`.json` 即可）或命令
- 你的系统、Python 版本、浏览器与版本
- 你期望发生什么、实际发生什么

## 使用时的安全边界

这个技能会做下面几件事，请确保你理解：

- **读取你给的文件**：文章、规格（`.json` / `.yaml`）。
  不要把不信任的规格丢进来跑 —— 规格里可以有 `raw` 版式，它会被当作 SVG 直接渲染。
- **启动本机浏览器**（Chrome / Edge / Chromium）做无头渲染，
  用一个临时 profile（系统临时目录下 `t2i-cdp-*`），结束后清理。
- **只在本地写文件**：HTML 中间产物在仓库的 `build/`，图片在你 `--out` 指定的目录。
- **不需要网络**：渲染全部离线完成。安装脚本会访问 GitHub / Gitee 拉取本仓库，
  那是唯一的联网动作。

## 我们不做的事

- 不读取、不上传你的文章或图片到任何地方。
- 不写密钥、不碰 `.env`（`install.sh` / `install.ps1` 也不会）。
- 不联网调用任何模型；本技能只做**确定性渲染**，生成靠脚本而不是 AI。

## 已知的使用注意

- `pyyaml` 是硬依赖。请用可信来源安装（`pip install pyyaml`）。
- 渲染用的是 `yaml.safe_load` / `json.loads`，不执行规格里的代码；
  但 `raw` 版式的 SVG 会被原样渲染，请只放你自己写的内容。
