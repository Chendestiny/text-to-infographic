# Changelog

格式参考 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)。
版本号遵循 [Semantic Ver](https://semver.org/)：渲染结果属于对外契约，
同样一份规格出新图不一样，算 breaking。

## [Unreleased]

### Added

- **`--theme` 命令行开关**（`run.py` / `pipeline.py`）：不写规格也能换皮肤，
  `--only N` 单页返工同样支持。命令行优先于规格里的 `meta.theme`。
- `make_style_probe.py` **方框样式比选**：固定版式与规格，只扫皮肤与节点令牌，
  把多个方案拼成一张表（列 = 皮肤，行 = 方案）。调节点外观时用它，
  比改完 `theme.py` 再跑整套图鉴快得多，也不会被版式差异搅浑判断。
- `make_theme_gallery.py --strips-out`：给每套皮肤出一条 **4 图横条**
  （封面 / 步骤链 / 左右对照 / 四象限）—— README 的「出图效果」改用这 7 条。
- **主题（皮肤）系统**：`meta.theme`（或 `--theme`）一行换掉整套视觉，内置 **7 套皮肤**，
  并且分成**两个族**（见下）：`crayon` 蜡笔纸感（默认，即原有风格）/ `grid` 方格稿纸 /
  `retro` 复古报刊（纸张族）；`terminal` 暗夜终端 / `blueprint` 工程蓝图 /
  `mono` 素白细线 / `nord` 北欧冷调（工程族）。
  令牌集中在新增的 `scripts/theme.py`（底色 / 描边 / 文字色 / 6 色调色板 /
  笔锋抖动 / 线宽 / 高亮形态 / 圆角投影 / 装饰件组合 / 节点外观 / 额外 CSS），
  **加一套皮肤只改这一个文件**。版式、字段、字数、四道门口径完全不变。
  - 四种高亮形态：`crayon` 蜡笔色带 / `flat` 半透明块 / `block` 实心块 / `underline` 粗下划线。
  - `wobble: 0` 的皮肤把整套手绘笔锋收成直线（终端的网格、蓝图的细线靠这一条成立）。
  - 主题名写错**当场报错**，不静默回落成默认皮肤。
- **两种新封面版式**：
  - `cover_title` 大字标题封面 —— 左对齐多行大标题（上限 124px，比页头 h1 更大）
    + 一条压在指定行下方的强调色条；`\n` 手动断行，`accent` 选第几行压条。
  - `cover_quote` 金句封面 —— 居中一句话 + 出处，上下各一条短色条框住。
  - 两者都**不走页头**（标题由版式自己画），由 `HEADERLESS` 集合控制。
- **拼版图鉴**：`make_layout_gallery.py` → `docs/images/layouts/版式图鉴.png`（14 张拼一张）；
  `make_theme_gallery.py` → 桌面 `t2i-themes/`（每套皮肤 8 张样张 + 拼版 + 总览），
  加 `--sheet-out` 出跨皮肤对比表、`--strips-out` 出单皮肤 4 图横条（README 的「出图效果」用它）。
  共用工具抽成 `scripts/sheet.py`。
- 装饰零件新增极客系四件：`plus3`（三个加号）/ `pixq`（像素问号）/
  `crosshair`（十字准星）/ `corners`（整卡取景框角）。

### Changed

- **皮肤分成两个族**（`theme.family`），因为这两类的**视觉语言根本不同**，不是换个颜色：
  - **纸张族 `paper`**：节点 = 手绘笔锋 + 上色方框，强调靠整块底色。复用现有版式，**已定稿**。
  - **工程族 `diagram`**：按用户定的三条口径重画节点 ——
    ① **框的背景透明**（`fill='none'`，网格直接透进框里）
    ② **边框黑色或主题色**（`node_line` 留空 = `ink`，不再用低饱和中间色）
    ③ **没有笔锋笔触**（精确 `<rect>`，不走"一条边拆 4 段收笔"）
    重音改为**四角刻度**（`node_style: corner`，每角一对短线，像取景框），
    `bullets` 的行也走同一套（`ink.node_mark_html` 与 SVG 侧一一对应）。
    金字塔的层轮廓改用统一主题色（结构线不是高亮）；深底的高亮透明度收到 `hl_alpha: 0.22`。
    线宽 `node_w` 抬到 1.7~4.8（1.3 那档实测太飘；北欧另按用户口径 ×2 到 4.8）。
    **版式仍在调整**（预览：`make_theme_gallery.py --family diagram`）。
  - `node_style` 一共六种形态，换一个令牌即可：`corner` 四角刻度 / `tab` 左上角块 /
    `rail` 左侧竖轨 / `rule` 顶边短线 / `double` 内外双线框 / `none` 不画。
    终端与蓝图用 `corner`，素白细线与北欧冷调**只用细线**（用户口径）；
    北欧的线宽单独加粗到 4.8（它底色偏灰，2.4px 在那里"飘"）。

  族的差异集中在一个新的节点原语 `ink.node_box()` 上：版式只说"这里有个节点、
  强调色是哪个"，由族决定怎么画。**踩过的坑**：一开始只换颜色，把纸张族那套
  高饱和方框填色直接套到工程风上 —— 暗夜终端里一块块荧光填充块，跟"工程图"完全打架。
- **删掉两套皮肤**：`morandi`（与 `crayon`/`grid` 高度重叠）与 `brutal`
  （高饱和色块在工程语义下不成立）。
- `bullets` 行的边框色改为跟随皮肤（`%ROW_LINE%`）：纸张族用墨迹色、工程族用节点描边色 ——
  用荧光绿描一整圈 row 边框会很吵。
- README 不再一行一张缩略图：版式与皮肤都改成**一张拼版**。
  图片表格在手机上会把页面撑成瀑布，而且每个 `<img>` 都是一次请求。
- 图鉴样张的**封面改成与整套样张同一篇文章**（Agent 工程）——
  原来封面写着"Qoder 和 Zcode 二选一"、后七张讲 Agent，放在拼版里一眼就是拼凑的。
- **DOM 文字槽的左右对齐此前一直是失效的**：`render_card` 用 `align == "left"` 判，
  而 `txt()` / `hl_line()` 传进来的是 `"start"`，两边永远不相等 ——
  spectrum 的两端标签、matrix 的轴标签其实一直以居中渲染。
  现在 `_reg_slot` 把 `start/end/middle` 与 `left/right/center` 两套写法统一归一化。
- **上色方框里的文字色改为按填充色亮度自动反差**（`ink._ink_on`）：
  亮底写深字、暗底写浅字。深色皮肤配高饱和填充时这是必须的 ——
  实测终端主题的浅灰正文压在琥珀/青/粉块上完全读不出来。
- 装饰件由皮肤决定：`decos` 空列表 = 不画装饰；半径上限 `DECO_MAX_R = 40`
  （内容区两侧各只有 92px 留白带，再大会探进正文）。

### Fixed

- **`fill: none` 一直是失效的**：十几处写的是 `PAL.get(x) or pal.next()`，
  于是 `"fill": "none"` 落到 `PAL.get("none")` = None → `or pal.next()` → **照样上色**，
  和文档承诺的「留白表达这一步不重要」正好相反。现在统一走 `ink.pick_fill()`，
  三态（没写 / none / 颜色名）显式判，`none` 明确不上色且不消耗轮换次序。
- 工程族的节点没有填充，但版式里仍按 `plain=bool(fill)` 剥高亮 → 一换族高亮全没了。
  新增 `ink.fill_plain()`，判据改成「这块底色上还能不能看出色带」。
- **`[[词|gr]]` 与所有"非默认色"的高亮一直是透明的**（潜伏很久，四道门全都没响）：
  `_inline` 把 `gr` 经 `COLOR_KEY` 映射成**颜色名** `green` 塞进 class，
  而 CSS 里只定义了 `.hl.gr` —— 于是 `[[词|b]]`（文档里明确宣传的写法）和
  `hl_line` 重建出来的彩色高亮**全都没有底色**；只有不带 `|` 的默认黄是好的。
  文字在、位置对、不压线，所以内容门与几何门都抓不到。
  现在 group(2) 本身就当 CSS 类名用，并新增一条测试钉住"每个 `.hl` class
  都必须命中一条 CSS 规则"（做过变异验证：注回旧写法测试立刻 FAIL）。
- `validate.eff_len()` / `_check_slot()` 遇到**非字符串字段**（如 `cover_title.accent: 2`）
  会抛 `TypeError`，整道规格门崩掉 —— 而 `run.py` 只看"有没有结论行"，
  崩了会落进「有提示，见上」，等于门又白跑了。现在非字符串直接跳过字数判定。
- 装饰件的描边色/抖动此前写死在 `decor.py` 的 def 期默认参数里（`color=INK`），
  换皮肤换不动。改成 `color=None` + 调用时回落，并加 `decor.set_style()`
  由 `ink.py` 在画装饰层前注入。
- 拼版图的两处排版毛病：角标不截断（`13 cover_title · 大字标题封面` 会压进下一格被裁半个字）、
  顶部小注左对齐会和标题叠在一起。

## [0.1.0] — 2026-09-21

首次公开发布。

### Added

- **四道门 + 一条命令**：文字预检 / 规格门 / 像素门（含几何布局门）/ 内容门，
  全部收敛到 `run.py`，输出一句结论 + 可直接粘贴的交付报告。
- `--plan`：文章 → 建议张数 + 允许区间 + 每页骨架 + 可直接粘进 `meta.plan` 的一行。
- `--only N`：**单页返工**，只重建/重测/重出第 N 页（1~2 秒），其余页不动。
- `--budget`：写文案前先看每个槽的尺寸与起手字号。
- 13 种版式（cover / hub / chain / cycle / spectrum / timeline / flow / bullets /
  compare / matrix / pyramid / arch / raw），样张见 `docs/images/layouts/`。
- DOM 文字排版：折行、垂直居中、字号自适应交给浏览器，引擎只给容器矩形。
- 自测 `tests/run.py`（20 条，约 85 秒）+ CI（ubuntu / windows）。
- 中英双语 README、安装脚本（含 GitHub / Gitee 镜像、国内免代理）。

### Changed

- **渲染确定性**：脚本拥有每一个像素，同样一份规格永远出同一套图；
  LLM 只写规格，不参与出图。
- 装饰件重画（云朵改为三瓣错落 + 平底）、`bullets` 行间增加间隙。

### Fixed

这一批都属于「门没跑 / 门报错说不清 / 门给了假绿」，是 0.1.0 的主要工作量：

- `run.py` 的子进程**没有超时** —— 一道门挂住就无限等待（实测 10 次 4 次卡壳）。
  现在每阶段有超时预算，超时明确报错并退出。
- **渲染失败被静默吞掉**：`shoot()` 失败只进日志、退出码仍为 0，
  "8 张里 1 张没出图"被当成干净交付。现在点名缺哪张并退出非零。
- **渲染前先删光所有 PNG**：一张失败会把已出好的图一起删掉。改为成功后才清理残留。
- `dom-orphan`（末行孤字）被当成硬错误 → 整轮 `exit=1`，白跑返工。
  现在它是提示，明确标注"不影响交付"。
- **规格门从来没跑过**：`validate.py` 无条件 `import yaml`，缺 pyyaml 时直接退出，
  而 `run.py` 只看 `pipeline.py` 的退出码 → 报 `exit=0`。现在门跑不完算硬问题，
  且 `validate.py` 总是输出稳定结论行。
- `plan.py` 读错了 `meta.plan.note` 的键（它自己打印的模板写的是另一个）。
- `capacity.py --budget` 判的是恒空表 → 对已知会溢出的规格照样报"都放得下 ✓"。
- 报错丢失实测值：`needs_llm` 丢了 issue 的 `note`（像素值），只说"这条不行"。
- `pipeline.py` 有硬问题（needs_llm）时仍 `exit=0`；`render.py` 的 CLI 兜底
  缺 `--no-sandbox`（容器里两条路都起不来）。

### Known limitations

- `pyyaml` 是硬依赖（规格门要解析契约本体 `templates/contracts.yaml`），
  与规格写成 `.json` 还是 `.yaml` **无关**。
- 文字排版已迁移到 DOM，但 `ink._CAPS` / `_reg_cap` 那套管路还没清掉（见 ROADMAP）。
- `crosses-box` / `crosses-line` 这类几何问题目前只有合成数据的单元测试，
  还没有端到端的真实渲染反例。
