---
name: text-to-infographic
description: >
  Turn a long article into a set of ready-to-publish Xiaohongshu / Instagram style
  information cards — a cover plus one page per key point, 3:4 vertical, hand-drawn
  card style.
  Use when the user says "把这篇文章做成图 / 总结成自媒体多图 / 出一套小红书图文 /
  做成图文卡 / make a carousel from this article / turn this into infographic slides".
  Deterministic rendering, NOT AI image generation — text is always crisp, output is
  reproducible and every page stays hand-editable.
  当用户要求把文章/文档/笔记转成竖版多图、自媒体图文、信息图组、图文卡片时使用。
---

# text-to-infographic

把一篇长文变成一套可直接发布的竖版图文：封面 + 每页一个要点。

**关键定位**：这不是 AI 生图。图是**脚本确定性渲染**出来的——文字永远 100% 准确、
同一份规格永远出同一套图、任何一页都能手改。AI 生图做不到这三点（中文必糊）。

---

## 一、核心原则：你只出规格，不碰像素

**你（Agent）只做一件事：读懂文章，产出一份规格文件（`spec.json`）。**
所有字号、留白、描边、色带位置都由 `scripts/ink.py` 决定，你不要也不要试图去改它们。

这条纪律换来三件事：风格不飘、可复现、用户能手改。一旦你去手写 HTML/SVG，
这三件事全没了。

```
文章.md ──> [你] spec.json ──> validate.py ──> pipeline.py ──> PNG × N
              只做这一步        规格门（字数契约）  像素门（真实浏览器实测）+ 修正循环
```

**两道门都是纯脚本，不花 token。** 你的产出必须过规格门；像素门由流水线自己跑。
只有流水线把某几条文案升级成 `needs_llm` 时，才需要你回去改规格。

---

## 二、五步流程

### 第 1 步：环境自检

```bash
python scripts/doctor.py
```

Windows 上 `python` 不一定在 PATH 上——先试 `python --version`，
没有就改用 `py -3`（官方启动器，必然在 PATH 上）。**本文所有 `python xxx.py` 都按这个规则替换。**

有 FAIL 项先告诉用户，不要自己乱装东西（`pyyaml` 缺失就走 `.json` 规格，本来就不需要它）。

### 第 2 步：读文章，决定拆几页

先通读，然后**只挑能独立成页的要点**。判断标准：这个点单独拿出来，读者能不能看懂？

- **唯一权威区间：4~9 张（含封面）** —— 其中 **6~9 是小红书配图的平台偏好区**，短帖 4~5 张也正常；
  平台硬上限 18 张（再多发不出去）。`validate.py` 就按这个区间给提示：超预算报页数提示、
  到 13 张建议拆系列、超 18 张报硬违规。**区间只在本节定义一次，README / docs 一律引用这里**
- **张数 ≈ 1（封面）+ 章节数**，再加两条修正：
  **平均每节 < 400 汉字 → 两节并一页**；**全篇 < 1.5k 汉字 → 压到 4~5 张**；章节多于 8 就先合并
  - 实测三例：8 节 / 2.6k 字 → **5 张**（节太薄，两节并一页）；6 节 / 3.4k 字 → **7 张**；6 节 / 0.9k 字 → **4 张**
  - **不按行数算**：228 行的文章是 7 张，1000 行的文章也是 8~10 张，永远不是 100 张

| 文章规模 | 张数（含封面） | 怎么承载 |
|---|---|---|
| ≤1.5k 字 / 1~2 节 | 4–5 | 一页一个要点 |
| 1.5k~3k 字 / 2~3 节 | 5–6 | 一页一个小节 |
| 3k~6k 字 / 4~6 节 | 6–8 | 一页 1~2 个小节，坑合并成一页 `bullets` |
| 6k~10k 字 / 6~10 节 | 8–9 | 一页一个主题；再多就拆系列，别硬撑 |
| >10k 字 / 10+ 节 | **拆成系列**，每篇 6–8 张 | 不要单篇塞到 18 张 |

- 想多讲的地方**先换写法，再考虑加页**：一页 `bullets` 放 4~6 条、`chain` 走 5~6 步、
  `compare` 一页就是 4+4 条对照——密度靠版式承载，页数才压得住
- 超过 12 张的正确做法是**拆成系列**（每篇 6~8 张），不是把一篇堆到平台上限
- 第 1 页永远是 `cover`（封面）
- 中间每页一个主题，不贪心
- 最后一页放结论/清单/对照

### 第 3 步：选版式 + 按字数预算写文案

先打开 **`templates/contracts.yaml`** —— 那是每种版式**每个文字槽位的字数范围**。
写文案不是"写好文案再祈祷能放下"，而是在预算内总结。

| layout | 长什么样 | 什么时候用 |
|---|---|---|
| `cover` | **汇总封面**：标题 + 2×2 四宫格，每格一个微缩版式 | **只用于第一页** |
| `hub` | 标题 + 中心圆 + 2~3 个并列概念框（>3 自动竖排） | 讲"三件套"这类并列概念的任意页 |
| `chain` | 竖排方框 + 箭头，可套虚线框标阶段 | 流程、步骤（最常用） |
| `cycle` | 节点围成一圈 + 弧形箭头 | 循环（ReAct / PDCA） |
| `spectrum` | 渐变粗箭头 + 两端标签 + • 列表 | 两个极端之间的取舍 |
| `timeline` | 居中竖轴，节点文字左右轮流 | 阶段、演进 |
| `flow` | 横排节点 + 箭头，可带底部一排小框 | 链路、数据流 |
| `bullets` | • + 大标题 + 说明，行高自适应 | 坑 / 清单 |
| `compare` | 左右两列对照，内层块自适应填满 | 反例 vs 正解 |
| `matrix` | 2×2 四象限 | 按规模/维度选型 |
| `pyramid` | 梯形分层，从上到下逐层变宽 | 能力层级、优先级分层 |
| `arch` | 嵌套框 + 箭头 + 底部 chip 排 | 角色 / 结构 / 三件套 |
| `raw` | 直接给 SVG 片段 | 上面都表达不了时兜底 |

规格写法参照 `examples/json/` 里的现成规格（**用 .json 零依赖**；`examples/*.yaml` 只是等价的可读版）。

### 第 4 步：过规格门

```bash
python scripts/validate.py spec/<名字>.json
```

它会逐条核对字数契约，超了报**精确路径**：

```
card-03(chain).steps[1].text        17.5 > max 15   暴力检索 O(n)：全量必准，但扛不住量
```

**按路径改那一条，再跑，直到 0 硬违规。** 软约束（会提示「（软）」）是密度建议，
可以不改，但改短更好看。

### 第 5 步：跑流水线

```bash
python scripts/pipeline.py spec/<名字>.json -o out/<名字>
```

它自动做：build → **真实浏览器实测**（`getBBox` 量每个文本的真实宽度）→
对比方框清单 → 有溢出就校准宽度估算器 / 注入 `textLength` → 最多 3 轮 → 出 PNG。

如果最后打印 `⚠ 以下文案需要 LLM 重写`，说明那几条脚本压不下了：
**按报错把那几条改短，再跑一次**（这是你的兜底入口，不是失败）。

### 第 6 步：逐张自检 → 交付 + 汇报

**这一步不需要用户交代，必须自动做。** 两道门只保证"字没溢出、没被吞"，保证不了"好看"，
所以交付前要**逐张看图**（`read_image`），专找这几类门看不见的毛病：

| 查什么 | 典型现象 |
|---|---|
| 拉丁词被折断 | `schema` 断成 `sche` / `ma`（微缩四宫格最容易犯） |
| 孤字换行 | 备注折成两行，末行只剩 1~2 个字 |
| 被截断 | 文字以「…」结尾，后半句在图上根本没有 |
| 压字 / 出框 | 标题压住说明；文字贴在方框线上 |
| 数量词对不上 | 标题写"两件事"，页面却列了 4 条 |
| 模板残留 | 页面角落冒出 `-->` 之类的标记 |
| 字号过小 | 微缩格子里的小字在手机上读不动 |

发现就改规格重渲（改文案 / 换版式 / 减条数），**别交付带瑕疵的图**。
页数多的大套图可以并行开子任务复核（每个子任务只分几页，各看各的），你只看它们的结论。

然后汇报——把 `out/<名字>/` 交给用户，附这样一段（**门禁输出要逐字贴，别复述**）：

```
页数：7 页（封面 + 6 个内容页；按「1 + 章节数，平均每节 < 400 汉字两节并一页」算）
版式：01 cover / 02 bullets / 03 chain / 04 compare / 05 pyramid / 06 bullets / 07 chain
规格门：契约校验通过（7 页，在推荐区间 4~9 内，0 硬 / 0 软）
像素门：measure: 0 处溢出 / verify: 干净，进入渲染
自检：7 张逐张看过——card-03 孤字、card-06 说明被截断 → 已改文案重渲，复查通过
耗时：渲染 4.9s；写规格 + 复核约 X 分钟
遗留：无（需要 1 倍图 1080×1440 就 render.py --scale 1 重渲，体积小 60%）
```

如果跑了几轮才过、或还剩 `needs_llm` 项，如实说明——**别把"看起来没问题"当结论**。

---

## 三、规格格式与语法

```json
{
  "meta": { "footer": "", "frame": ["pen", "pen", "none"], "decor": true },
  "cards": [
    {
      "layout": "chain",
      "title": "Function Calling",
      "subtitle": "[[tool_call]] 是模型吐的，干活的是代码",
      "steps": [
        { "text": "① 注入 [[tools 定义]]" },
        { "text": "③ 模型返回 tool_calls", "fill": "yellow" }
      ],
      "note": "回到 ② 步，直到模型不再调工具"
    }
  ]
}
```

- `meta.frame`：每页外框轮换，`pen` 手绘框 / `card` 圆角卡 / `none` 无框
- `meta.decor`：齿轮 / 星形装饰开关（默认开）
- 单卡可覆盖 `frame`，也可用 `dashed: true` + `dashed_label` 加一层虚线容器（chain / flow 支持）
- 标题里写 `\n` 可以显式换行（长标题自己控制断点更好看）

### 高亮语法

文字里写 `[[关键词]]` 就给这段加马克笔底色；想指定颜色写 `[[关键词|b]]`：

| 写法 | 颜色 | 写法 | 颜色 |
|---|---|---|---|
| `[[x]]` 或 `[[x\|y]]` | 黄 | `[[x\|gr]]` | 薄荷绿 |
| `[[x\|b]]` | 蓝 | `[[x\|o]]` | 杏橙 |
| `[[x\|p]]` | 粉 | `[[x\|g]]` | 灰 |

不写颜色时**自动轮换**（蓝→黄→粉→绿→灰→橙），所以不会整页一种颜色。
显式指定的颜色优先，且不占用轮换次序。

### 上色纪律（和引擎行为一致）

- **方框默认全部上色，颜色自动轮换** —— 不需要你逐个指定 `fill`
- 想让某个方框留白表达"这一步不重要"，写 `"fill": "none"`
- 框已上色时，框内文字**不会**再画色带（避免色带和框色打架）——这是引擎行为，不用你处理

---

## 四、环境与能力自检

### 依赖清单

| 依赖 | 必需？ | 说明 |
|---|---|---|
| Python 3.8+ | 必需 | 核心脚本只用标准库 |
| pyyaml | **可选** | 只有 `.yaml` 规格需要；**`.json` 规格零第三方依赖** |
| Chromium 浏览器 | 渲染必需 | Windows 自带 Edge（doctor 自动识别）；否则 `T2I_BROWSER` 指定 |
| 字体 | 已内置 | 站酷快乐体（OFL 许可）随仓库分发 |

**Windows 的 `python` 兜底**：`py -3`（`C:\WINDOWS\py.exe`，必然在 PATH 上）。
实测最小 PATH 环境下 `py -3` 可用（Python 3.11 + pyyaml 齐全）。

### 多模态自检：先判定，再决定要不要做视觉终检

图片支持有**三种**状态，不是两种——**判定不能靠"harness 有没有声明"，要真读一张已知内容的图**：

| 状态 | 表现 | 你怎么做 |
|---|---|---|
| ✅ 能读 | 说得出图里的具体内容 | 做审美终检（抽查封面 + 一张内容页） |
| ⚠️ **静默丢图** | 请求成功、回答流畅，但模型**根本没看到图** | **按不能读处理**。这类最危险：会让你误以为"已视觉校验" |
| ❌ 纯文本 | 读图工具直接报错 | 跳过，如实说明 |

**先跑探针（3 秒，零依赖）**：

```bash
python scripts/vision_probe.py                 # 打印自带的探针图路径 + 该问什么
#   → 读 assets/vision-probe.png，回答：背景什么颜色？中间什么形状？底部还有什么？
python scripts/vision_probe.py --check "<模型的回答>"
```

探针图（`assets/vision-probe.png`）是**随仓库分发的固定资产**，内容恒定、可人工审阅，
不需要每次生成——判定标准才公平。

必须同时说出**红色**和**三角形**才算通过。只答"一张图""一张红色图片"→ 判为静默丢图。

**交付纪律（重要）**：

- 探针**没通过**，就不许在报告里写"视觉已检查/审美无误"——
  只能写「正确性已由脚本实测保证；审美未校验（当前模型无读图能力），请人工过目」
- 探针**通过**了，抽查 2~3 张（封面 + 一张内容页），不满意就改规格重渲
- 关键：**正确性校验（溢出）根本不需要视觉**——`measure.py` 用 `getBBox()` 量真实文本，
  纯文本模型读 JSON 报告就能完成整个校验闭环

已知实测（本机 DSH + 阿里百炼通道，仅供参考，各环境可能不同）：
`qwen3.8-flash` / `qwen3.8-max` / `deepseek-v4.1-flash` / `kimi-k2.7-code` 能读；
`glm-5.2` / `glm-5.3` / `MiniMax-M2.5` / `deepseek-v4-flash-0731` 属**静默丢图**。

### 纯小白兜底链

1. Python 没装 → 给 python.org 链接，装任意 3.8+
2. pyyaml 缺失 → 走 `.json` 规格（零依赖），或 `pip install pyyaml`
3. 浏览器找不到 → Windows 自带 Edge 通常能命中；否则 `T2I_BROWSER` 指定
4. 什么都不想装 → 把 build 出的 **`.html` 直接发给用户**：浏览器打开就是成品，
   手动截图也能发——自动化截图是锦上添花，不是唯一出口

---

## 四·五、细节手册在 docs/

本文只给流程和纪律。**遇到具体问题时查 `docs/`**：

| 问题 | 去哪 |
|---|---|
| 某个版式有哪些字段、能放多少字 | `docs/layouts.md` |
| 排障：截图失败 / 溢出 / 契约报错 / 环境缺失 | `docs/troubleshooting.md` |
| 渲染后端（CDP vs CLI）、profile 冲突这个坑 | `docs/rendering.md` |
| 契约怎么读、硬约束软约束的区别 | `docs/contracts.md` |
| 为什么这么设计（架构与两道门） | `docs/architecture.md` |
| 想加一种新版式 | `docs/extending.md` |

## 五、常见坑（都是实测踩出来的）

1. **文案超契约只有两种下场**：SVG 直排文本会**溢出或被缩字**；HTML 槽位会**自动换行**。
   前者难看，后者还行——但都别依赖，宁可拆页。
2. **画到画布外的内容会被静默裁掉**，不报错。`raw` 页要自己算高度。
3. **YAML 的 `{}` 内联写法别换行**，缩进层级容易错。**能用 .json 就用 .json**。
4. **Windows 路径**：`render.py` 内部用 `os.path.abspath()` 处理，
   不要自己拼 POSIX 风格的 `/c/...` —— Chromium 会静默不产出。
5. **改完 HTML 不生效**：Chrome 会把 `file://` 页面缓存进 profile；
   `render.py` 每次用全新临时 profile，已规避。
6. **不要手写 HTML/SVG 再让脚本"照着渲染"**——流水线只认规格。要特殊页用 `raw`。
7. **所有截图都失败 / 返回 0 字节**：先跑 `python scripts/doctor.py`，它的**后端预检**
   会实测 CDP 和 CLI 哪个能出图并给出耗时。受限沙箱常拦管道的 stdout（报
   `CreatePipe permission denied`），此时设 `T2I_BACKEND=cdp`（CDP 走 WebSocket 不受影响）。
   ⚠ 别把这种情况误判成"Chrome 坏了"——CLI 本身通常没坏，只是更容易被环境拦。
8. **页面底部被裁**：清单页条数超了。bullets 行高由浏览器分配（不会溢出），
   但条数一多字号会缩到不可读；契约里 bullets 上限 6 条，超了就拆页。
   `measure.py` 的 `v-overflow` 会报「N 行 HTML 行撑破卡片」，那是让你减条数或拆页的信号。

---

## 六、交付检查清单

- [ ] `validate.py` **0 硬违规**
- [ ] `pipeline.py` 最后没有 `needs_llm` 清单（有就先改文案再跑）
- [ ] 页数在 4~9 之间（唯一定义见第 2 步；6~9 是平台偏好区，平台上限 18）
- [ ] **逐张看过图**（断词 / 孤字 / 被截断 / 压字 / 数量词对不上 / 模板残留），发现的都已改文案重渲
- [ ] 交付时给了门禁逐字输出 + 页数理由 + 自检结论，没把"看起来没问题"当结论

## 七、输出与发布建议

- 产物默认 1080×1440（3:4 竖版）的 **2 倍图**，即 2160×2880
- 小红书 / Instagram / LinkedIn 的轮播都吃这个比例
- 多图按 `card-01`、`card-02`… 顺序发布，就是一套完整的轮播
- 用户想调风格时，改 `assets/style.md` 里记录的常量，
  **不要**改单页的 HTML——一页一改风格就飘了
